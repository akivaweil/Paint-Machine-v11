#ifndef GLOBAL_STATE_H
#define GLOBAL_STATE_H

extern bool isPaused; // True if the machine is paused by user
extern bool isActivePainting; // True if any painting operation is active (state machine or individual)

#endif // GLOBAL_STATE_H 